const myObject={
    js:'java'
    ,cpp:'c++'
    ,rb:'ruby'
    ,swift:'swift ny appple'
}

for (const key in myObject) {
  console.log(`${key} shortcut if for ${myObject[key]}`);
}

const programming= ["js",'rb',"py"]

for (const key in programming) {
        console.log(programming[key])
}